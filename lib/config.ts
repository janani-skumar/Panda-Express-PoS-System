/**
 * Application-wide configuration constants
 * Modify these values to change app behavior globally
 */
export const APP_CONFIG = {
  /**
   * Maximum number of items a customer can order at the kiosk
   * This prevents excessively large orders that would need to be handled at the counter
   */
  KIOSK_MAX_ITEMS: 10,
};

